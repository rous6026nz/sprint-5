/* ---------------------------------------
    EXERCISE 1: LOOPING A TRIANGLE
--------------------------------------- */

// Your code here.
// initiate bindings.
let str = "",
    i = 0;
while(i < 7) {
  // add an extra hash to the string.
  str = str + "#";
  // output the string to the console.
  console.log(str);
  // increment the count.
  i ++;
}